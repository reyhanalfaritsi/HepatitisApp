<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hepatitisapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HepatitisApp">
        <activity
            android:name=".InformationHepatitisActivity"
            android:exported="false"
            android:label="Pengertian Hepatitis"
            android:parentActivityName=".HomeActivity"/>
        <activity
            android:name=".GejalaActivity"
            android:exported="false"
            android:label="Macam-Macam Gejala Hepatitis"
            android:parentActivityName=".HomeActivity"/>
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResultPredictionActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="Form Prediction"
            android:parentActivityName=".HomeActivity" />
    </application>

</manifest>